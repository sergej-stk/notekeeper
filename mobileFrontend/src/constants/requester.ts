import { RequesterOptions } from "@/src/app/hooks/net/useRequester";

const settings: RequesterOptions = {
  endpoint: "notekeeper.ckissmann.de/api/v3",
  protocol: "https",
};

export default settings;
