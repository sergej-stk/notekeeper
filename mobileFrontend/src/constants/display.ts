import { DisplayResolutionSettings } from "@/src/app/hooks/useDisplay";

const settings: DisplayResolutionSettings = {
  mobileBreakpoint: "md",
  md: {
    min: 0,
    max: 500,
  },
  sm: {
    min: 0,
    max: 200,
  },
};

export default settings;
